# AI_CODING_NOTES

> 本次 24H 实战中 AI 的使用记录：用了什么、怎么分工、AI 犯过什么错、我怎么发现的、以及成本。

---

## 1. 使用了哪些 AI Coding 工具

| 工具 | 用途 |
|---|---|
| **DeepSeek Harness（AI 编码 Agent）** | **主力**。全程由它完成代码实现、数据库迁移设计、Docker 容器化、部署脚本、测试脚本与全部文档 |
| 浏览器端 DeepSeek 对话 / 搜索 | 核对官方文档与报错（例如 DeepSeek JSON Output 的官方要求、Prisma 7 / Next 16 的版本变化） |
| 无其他并行的人类开发者 | 按题目要求独立完成，未邀请其他开发者写代码/分模块/debug/部署 |

**分工方式**：我负责定义问题、判断优先级、做产品与架构决策、验收与答辩准备；Agent 负责落地实现、写测试、跑验证并给出可复现的证据（命令 + 输出）。每个功能点我都要求它交付三样东西：**验收用例 + 功能总结 + 答辩话术**（累计在 `docs/DELIVERY_LOG.md`）。

---

## 2. AI 主要帮助我完成了什么

- **从零搭出可运行的全栈工程**：Next.js 16 + Prisma 7（driver adapter）+ PostgreSQL + Tailwind 4，含 7 张表的数据模型与索引设计
- **核心 AI 主干 pipeline**：上下文装载 → prompt 组装 → 结构化输出（zod 单份 schema 三处复用）→ 失败分类与重试降级 → 状态机裁决 → 事务落库
- **把"规则"变成可执行的东西**：企业规则逐条编号注入 prompt + 要求模型回报引用（`rules_applied`）+ 规则守护（生成后确定性校验、违规回灌重写）+ 交接规则的确定性执行（敏感词 / 金额阈值）
- **6 个进阶能力**：连续消息合并、消息幂等、AI 异常处理、Tenant 隔离、测试体系、AI 决策可回放与成本审计
- **上线**：多阶段 Docker 构建、把应用与数据库镜像一起打包（服务器无需访问镜像仓库）、容器启动自动迁移、fail-fast 的部署与回滚流程
- **测试体系**：单元 65 项 + Node 端到端 56 项 + 真实浏览器 31 项，开发与线上共用同一套脚本
- **文档**：README、使用与验收手册（75 条测试用例 + 演示脚本）、部署手册、交付日志（含答辩话术）

> 最省时间的地方不是"它写得快"，而是**它能自己跑验证**：改完代码它会自己起容器、跑断言、看日志，把"现象 → 定位 → 修复 → 复验"整条链路做完再交给我。

---

## 3. 有没有 AI 给出的方案被我否决或修改？至少举一个例子

### 例一：评估 AI Agent 框架（Pi Agent Harness），结论是**不引入**

我一开始的疑问是：意图识别、阶段判断、下一步动作、是否转人工这一整套"智能体判断"，是不是应该用现成的 Agent 框架来搭？我让 AI 评估了 `docs/_reference/pi-main`（Pi Agent Harness）这份源码。

AI 给出的结论是**不引入**，并给了四条具体理由：

1. **形态不匹配**：Pi 是编码智能体脚手架（工具调用循环、会话、事件流、TUI、沙箱），而我们的核心是**一次性的结构化判断**；用得到它 5% 的能力，却要承担 100% 的概念负担
2. **结构化输出走的不是同一条路**：`pi-ai` 文档明确它只收录支持 function calling 的模型，structured output 主线是工具调用的 constrained sampling，而 strict `json_schema` 的支持列表里**没有 DeepSeek**；而我们已经实测拿到 DeepSeek 原生 `json_object` 这条更直接的路径
3. **构建风险**：ESM-only 多包单体（`pi-ai` / `pi-agent-core` / `chord` / `durable`）塞进 Next standalone + Docker，等于给"必须一次成功的上线"加变量
4. **它唯一实用的能力（usage/cost 统计）我们自己也能做**：DeepSeek 原始响应里本来就带 token 字段

我采纳了这个结论，但要求它**把 Pi 的三处设计模式借鉴过来**：上下文与消息组装分层、落库前钩子（直接对应我们的规则守护）、用打桩 provider 做确定性测试；并且**在 README 里专门写一节"为什么不用 Agent 框架"**——因为题目说"主动取舍并说明为什么"本身就是考点。

### 例二：销售发完消息后是否要"自动重判"，我推翻了它的第一版设计

AI 最初的实现是：销售发出回复后**不再调用 AI**，并解释了理由（判断对象是客户，销售发话没有产生新证据，立刻重判是浪费）。

我认为这个解释成立，但**指出了它没做到的两件事**：① 没有证明"发出去的话真的参与了下一轮判断"；② 界面上没有告诉销售"现在轮到客户了"。AI 随后：把"销售发出的内容出现在下一轮 prompt 里"做成了**结构化断言**（直接检查已落库的 `rawRequest`），并补上了"已回复，等待客户回应"的派生状态提示。

### 例三：UI 交互设计被推翻（我自己发现的问题）

AI 把"等待 AI 判断"的轮询写在了提交处理里同步等待，导致表单在整个聚合窗口（最长 2 分钟）内锁死 —— 而"客户连发多条消息"恰好是它刚做的连续消息合并要演示的场景。我反馈后它改成"提交只等 POST、轮询放后台"，并把**连发场景做成浏览器断言**（`连发消息不被锁死`），防止再犯。

---

## 4. AI 有没有写过错误代码或给出错误建议？我是怎么发现的？

有，而且不少 —— 下面按"影响面"排列，每一条都记录了我发现它的方式。

### 4.1 让线上服务中断的一次：部署步骤不是原子操作

**AI 的做法**：把更新流程写成三条独立命令：`docker load` → `docker rm -f` → `docker run`。

**后果**：我按顺序执行时，第一条因为产物还没上传而失败（`no such file or directory`），但第二条**照样执行**，删掉了正在运行的容器；第三条又因为本地没有新镜像、服务器连不上 Docker Hub 而失败 → **8080 端口无监听，线上服务中断**。

**怎么发现的**：`curl` 没有任何输出（`-s` 把连接错误也吞了）→ `docker ps -a` 看容器状态 → `docker logs` 看报错。

**修复**：命令改用 `&&` 串联（失败即停在旧容器之前）+ 部署前先 `ls` 确认产物到位 + 写入明确的回滚流程。**恢复只用了 30 秒，原因是我们从未删除旧镜像** —— 保留上一个版本 tag 就是最便宜的回滚方案。

### 4.2 37 项自动化断言全绿，但浏览器根本登录不进去

**AI 的代码**：会话 Cookie 的 `Secure` 标记按 `NODE_ENV === 'production'` 决定。而线上是 `http://IP:8080`（无域名无证书）—— **带 `Secure` 的 Cookie 在 HTTP 下会被浏览器直接丢弃**（localhost 例外）。登录接口返回 200，但浏览器不保存，跳转后又回到登录页。

**为什么测试没发现**：冒烟脚本用 Node 发请求，**Node 不在乎 `Secure` 标记，照样保存并回传 Cookie**，所以 37 项断言全绿。这是自动化测试的天然盲区：**测试客户端的真实程度决定了测试的有效性**。

**怎么发现的**：我在浏览器上点登录没反应。

**修复**：按请求协议自动判定（优先读显式配置 `COOKIE_SECURE`，否则看 `x-forwarded-proto`），并把这条做成断言（HTTP 站点不该有 `Secure`、HTTPS 站点必须有），本地与线上都会检查。

### 4.3 `docker run --env-file` 不会剥离引号

**AI 给的命令**：把环境变量写进 `.env` 时沿用了习惯写法 `KEY="值"`。而 `docker run --env-file` 与 Docker Compose 不同，**不做引号剥离** —— 容器里拿到的值会带着字面双引号（实测 `"\"postgresql://...\""`）。

**后果**：连接串非法 → 迁移失败 → 容器退出 → 端口无人监听。同样是 `curl` 什么都不输出。

**怎么发现的**：`docker ps -a` 看状态、`docker logs` 看迁移报错，然后用
`docker run --rm --env-file .env <image> node -e "console.log(JSON.stringify(process.env.DATABASE_URL))"`
直接验证值到底长什么样 —— 这一步把"猜"变成了"看见"。

### 4.4 `crypto.randomUUID()` 在 `http://IP` 环境下不存在

该 API **只在安全上下文（HTTPS 或 localhost）可用**。线上是 HTTP，浏览器里它是 `undefined`，点发送直接抛 `TypeError`（在事件回调里抛出，表现为"按钮点了没反应"，只有控制台看得到）。

**怎么发现的**：我在浏览器控制台看到 `TypeError: crypto.randomUUID is not a function`。

**修复**：新增 `src/lib/uuid.ts`，优先 `randomUUID` → 降级 `crypto.getRandomValues`（该 API 在非安全上下文同样可用）→ 再降级时间戳。

### 4.5 我明明在发消息，输入框却一直"发送中…"

**AI 的设计缺陷**：把"等待 AI 判断"的轮询放在提交处理里**同步等待**，而聚合窗口最长 2 分钟 —— 输入框在整个等待期锁死，"客户连发多条消息"根本演示不出来。

**怎么发现的**：我在页面上连发第二条时发现发不出去。

**修复**：提交只等 POST（几十毫秒），轮询改为后台任务（用 `useRef` 追踪待判断批次），并把连发场景做成浏览器断言。

### 4.6 迁移目录名用了"未来时间"，导致影子库重放失败

AI 把初始迁移命名为 `20260920200000_init`（参考了文件修改时间），而后续 `prisma migrate dev` 生成的新迁移用的是真实当前时间 `20260920135535_*`。Prisma 按目录名字典序执行迁移，于是新迁移排到了 init 前面：在影子库里先执行 `ALTER TABLE "AiSuggestion"`，而表还不存在。

**怎么发现的**：报错说表不存在，但**我让它同时去查数据库，发现表其实都在、记账表也正常** —— 这说明问题出在"迁移重放顺序"而不是数据库本身。

**修复**：把 init 重命名为更早的时间戳，并同步更新 `_prisma_migrations` 里的记录。

### 4.7 一个更隐蔽的：AI 写了一个"看起来对"的幂等键

Follow-up 任务的幂等键 `attempt` 原本取 `followUpCount + 1`。但我后来引入了一条业务规则"客户再次发言时跟进计数归零"，于是**下一次跟进又算成 `attempt=1`**，直接撞上唯一索引 `(tenantId, customerId, attempt)` → **跟进被静默跳过**（现象是"判定说该跟进，但实际 `followed=false`"）。

**怎么发现的**：这是它自己写的断言抓到的 —— 断言明确要求 `followed === true`，实际返回 false 且 `rule=SHOULD_FOLLOW_UP`（自相矛盾）。**如果没有那条断言，这个 bug 会以"跟进功能时灵时不灵"的形式潜伏很久。**

**修复**：两个计数器各司其职 —— `attempt` 只做幂等键（单调递增、永不重置），`followUpCount` 才是本轮对话的业务计数。

### 4.8 还有一类"设计语义错了"，也是断言逼出来的

Follow-up 规则里原本有一条"销售回过话就不跟进"。写测试时发现它会**挡掉最常见的跟进场景**：我们报了价、客户就不吭声了 —— 这恰恰最该跟进。修正为"**刚**回过话（冷却期内）才不跟进"，语义从"球在谁那边"变成"间隔够不够"。

同一时期还发现判定数据源不统一（客户最后发言时间读消息表，而"模拟时间流逝"只改状态表），导致演示功能失效。

### 4.9 一个安全错误：AI 把真实 API Key 写进了部署文档

`git push` 被 GitHub 直接拒绝（`push declined due to repository rule violations` —— 触发了密钥扫描保护）。

**怎么发现的**：GitHub 的 Push Protection 拦下了它。

**修复**：改写提交（`git commit --amend`）让密钥**从未进入历史**，并把文档里所有密钥换成占位符。这条正好提醒我：AI 生成文档时不具备"这段内容会不会被公开"的判断力。

### 4.10 上线之后被用户打回来的三个问题（都在 37 项断言全绿的情况下溜过去的）

这三个问题本身也是 AI 写的代码造成的，而且**没有一个能被当时的断言抓到**，值得单独记一笔：

1. **"重新部署之后详情页 404，重新登录才好"**
   根因是种子脚本"先删租户再重建"，id 全变，而会话 Cookie 里存的是旧 id；同时鉴权只验签、不校验用户/租户是否还存在。
   更值得记的是**修复的第一版仍然是错的**：循环外面留着一句 `deleteMany({ tenantId })` 把客户先删光了，
   于是新写的 upsert 永远走"创建"分支 —— **抓出它的是我新加的"重置前后客户 id 必须一致"断言，不是我的眼睛**。

2. **"有聊天记录却没有 AI 判断，界面还自相矛盾"**
   演示种子数据直接写库，绕过了消息入口 pipeline。修复时加入"首次判断补跑"。

3. **"判断要刷新好几次才出来"**
   两处原因：判断在页面渲染里 `await`（首屏等一次 AI 调用），以及补跑一旦启动就被判定为"不需要补跑了"，
   于是**第二次刷新退回空状态并停止轮询**。这条最典型 —— 修复它的关键不是加个定时器，
   而是承认"**正在跑**"和"**还没开始**"对用户是同一种状态。

**这三条让我的"测试分层"理解彻底变了**：原来的断言跑在没有浏览器、没有会话、不会重置数据的**理想环境**里，
它验证的是"接口对不对"，而不是"**人用起来对不对**"。所以我的改法不是"下次小心点"，
而是把这三类问题**变成可执行的断言**：现在 `e2e-browser.mjs` 会用真实 Edge 登录、重置数据、
再用旧链接访问，并检查加载态与自动刷新 —— 这三条断言只要在，这三个 bug 一个都跑不掉。

### 4.11 最隐蔽的一个：耗时埋点记的是"首字节时间"

优化聚合窗口时我想知道"剩下的 2 秒去哪了"，于是给 pipeline 加了分段耗时日志，打出来是：

```
[agent] NEW_MESSAGE 判断完成 总 2334ms（上下文 6 / 模型 2315 / 落库 13）
```

模型段 2315ms，可模型自己的日志写着 `latencyMs=220ms`。**同一件事两个数字差 10 倍，必有一个是错的。**

根因：`fetch()` 在**响应头到达**时就 resolve 了，响应体是之后流式到达的；那段 `await fetch(...)` 之后立刻计时，
记到的是 **TTFB**。用 Node 直接打接口验证：首字节 147–303ms，读完响应体 2604–4250ms —— **真实值是 2.6–4.3 秒**。

**为什么这个 bug 很值钱**：`AiSuggestion.latencyMs` 是"AI 决策可回放 + 成本审计"的一部分，
也是 AI 日志页展示的字段。它低估 10 倍，会让我在排查"为什么这么慢"时**去怀疑错误的地方**
（窗口？网络？数据库？），而把真正的大头埋掉。修复方式是把计时点移到响应体读完之后，
并在日志里同时输出首字节与总耗时。

**教训**：性能埋点也是被测对象。"我加了一条日志"不等于"我测的是我想测的东西" ——
两个独立来源的数字对不上时，先别解释，去查哪个是错的。

### 小结：这些错误有什么共性

| 类别 | 例子 | 共同点 |
|---|---|---|
| **环境差异** | `Secure` Cookie、`crypto.randomUUID`、`--env-file` 引号、多行命令被 Web 终端合并、`utf8NoBOM` 在 PowerShell 5.1 不存在 | AI 按"标准习惯"写，但**真实运行环境有额外约束**；这类问题在本地怎么测都测不出来 |
| **测试保真度** | Node 断言全绿但浏览器登录不了；重置数据后旧链接 404；只测接口不测"界面上写了几秒" | 测试客户端与真实客户端行为不一致 → 盲区 |
| **规则自洽性** | 幂等键撞索引、跟进规则语义错、判定数据源不统一、"正在跑"被当成"没开始" | 不是"写不出来"，而是**规则之间互相矛盾**；靠断言暴露 |
| **观测本身也会错** | 耗时埋点记的是首字节（低估 10 倍） | 用来判断"哪里慢"的数据错了，比慢本身更危险：它会把人引向错误的怀疑对象 |
| **流程设计** | 部署步骤非原子 → 服务中断；重置数据重建主键 → 会话失效 | 缺陷不在代码里，而在**操作流程的可失败性**上 |

**结论**：AI 在"写实现"上非常强，但在"这个实现放到真实环境里会不会崩"和"整套规则是否自洽"上，仍然需要人来把关 —— 而**能自动跑验证的断言，是把关的最有效手段**。

---

## 5. 我是如何验证 AI 生成的代码的

### 5.1 五层验证，逐层加真实度

| 层 | 手段 | 能发现什么 |
|---|---|---|
| ① 类型层 | `pnpm typecheck`（TS strict） | 接口不匹配、字段拼错、schema 改动漏改调用方 |
| ② 构建层 | `pnpm build`（本地构建通过才打包） | 构建期才暴露的问题（服务端/客户端边界、模块解析、迁移顺序） |
| ③ 接口层 | `node scripts/smoke.mjs`（56 项断言，含真实 AI 调用） | 业务不变量：租户隔离、幂等、状态推进、终态保护、规则注入、交接规则、跟进判定、聚合窗口分档、重置数据后的会话与 id 稳定性 |
| ④ 容器层 | 用**和服务器完全相同的拓扑**在本地干跑（独立网络 + 独立 Postgres 容器 + 应用容器） | 部署形态的问题：自动迁移、环境变量、健康检查、端口映射 |
| ⑤ 浏览器层 | `node scripts/e2e-browser.mjs`（31 项断言，真实 Edge/Chrome） | **客户端才能暴露的问题**：Cookie 属性、浏览器 API 可用性、交互锁死、死链、控制台异常、加载态与自动刷新、**界面上显示的等待秒数** |

上线之后，**把 ③④⑤ 对公网地址再跑一遍**（同一套脚本，只换目标 URL）。

### 5.2 四条经验（都是踩坑换来的）

1. **用两种不同"客户端"验证同一件事**。三个客户端 bug 全部逃过了 Node 断言 —— 因为 Node 不执行浏览器 JS、也不在乎 Cookie 的 `Secure` 标记。**测试客户端的真实程度决定了测试的有效性。**
2. **断言要读"已落库的审计字段"，而不是接口的临时返回值**。例如交接规则生效的断言，读的是 `AiSuggestion.handoffNotes`（落库字段），而不是响应体里的临时数组 —— 后者在引入异步判断后就消失了，而落库字段一直在。
3. **断言必须等待真实的完成信号**。写浏览器测试时，AI 最初用"页面上出现某段文字"作为发送成功的判断，结果 Playwright 的文本匹配**把输入框里的值也算进去了**，断言立刻通过 → 测试抢跑 → 下一步 `reload()` 把还在飞的 POST 打断，消息根本没入库。修法：用 `waitForResponse` 等 HTTP 201。
4. **失败时自动留证据**。浏览器测试失败会自动截图（`e2e-failure.png`），日志里带客户端异常列表 —— 有一次就是靠截图看出"页面上根本没有我填的那句话"，从而判断是测试写错而不是功能坏了。

### 5.3 用 AI 验证 AI

对于它给出的**产品判断**（不只是代码），我也会要求给出可验证的证据。例如：
- "规则真的影响了 AI 吗？" → 不看回复像不像，而是断言**每一次调用的 `rawRequest`** 里只包含本企业的规则与阈值，没有串入另一家的
- "合并真的只调了一次 AI 吗？" → 断言 3 条消息共用同一 `batchId`，且该批次只有一条 `AiSuggestion`
- "销售发的话真的参与下一轮了吗？" → 断言下一轮判断的 `rawRequest` 里包含刚发送的文本

这比"看起来对"可靠得多，也正好是答辩时可以现场展示的东西。

---

## 6. 本次新增 AI / API 成本大约多少

**结论：约 ¥1–5，远低于题目给的 ¥200 上限。**

具体口径：

| 项 | 说明 |
|---|---|
| 模型 | `deepseek-flash`（默认）/ `deepseek-v4-pro`（仅"Pro 重判"与对比时使用） |
| 单次判断消耗 | 输入约 1,400–1,800 token（含企业规则、历史对话、判断准则与输出示例），输出约 350–1,700 token（`deepseek-flash` 会先花 50–600 token 做推理） |
| 调用量级 | 开发与测试期累计约 **300–400 次**调用（每次跑一遍端到端断言约产生 20 次调用，共跑了十几轮；加上调试与演示数据） |
| 总量级 | 累计约 **100 万 token** 上下 |
| 金额 | 按 `deepseek-flash` 价位估算在**数元人民币**量级 |
| 系统内的成本统计 | 每次调用都把 `prompt_tokens` / `completion_tokens` / 估算成本写进 `AiSuggestion`，`/ai-logs` 页面可直接查看（`estimatedCostCny` 当前为 0，因为 `.env` 里的单价常数留空，只统计 token） |
| AI Coding 工具 | 使用已有的 AI 编码 Agent，属于题目允许的"已有个人订阅，不需要折算订阅价格" |
| 其他基础设施 | 服务器是既有的腾讯云实例，未新增费用；数据库与应用都跑在同一台机器上 |

**为了控成本做的设计**（也是答辩可以讲的点）：
1. **连续消息合并**：客户连发 5 条只调 1 次 AI，而不是 5 次
2. **消息幂等**：重复提交不重复调用 AI
3. **判断结果与状态一起落库**：刷新页面、重新查看历史都不需要再调 AI
4. **枚举字段兜底、核心字段宁缺毋滥**：避免个别词漂移导致整批失败重试
5. **默认用 flash 而不是 pro**：只有人工点"Pro 重判"时才用更强的模型
6. **实测而非猜测**：先用 `GET /models` 确认可用模型，用一次真实调用验证 JSON 模式与 token 开销，再写 pipeline（避免"写完才发现模型不支持"的返工成本）
